{
    "name": "format_date_time_moment",
    "version": "1.4.0",
    "description": "The purpose is to ensure that one can format date and time variables, given whatever the format may be in",
    "main": "index.js",
    "directories": {
        "example": "example",
        "test": "test"
    },
    "scripts": {
        "update": "npm version minor",
        "push": "git add . && git commit -m 'updated' && git push",
        "start": "npm run update && npm run push && npm publish",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "",
    "license": "MIT"
}
